<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FreshCart - Online Grocery Store</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* Font Import */
        @import url('https://fonts.googleapis.com/css2?family=Jost:wght@300;400;500;600;700&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&display=swap');

        /* Color Variables */
        :root {
            --primary: #000000;
            --secondary: #333333;
            --text: #666666;
            --light: #f8f8f8;
            --accent: #e8e8e8;
            --white: #FFFFFF;
            --black: #000000;
            --border: #e0e0e0;
        }

        /* Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            color: var(--text);
            line-height: 1.6;
            font-size: 16px;
            font-family: 'Jost', sans-serif;
        }

        h1,
        h2,
        h3 {
            font-family: 'Playfair Display', serif;
            color: var(--primary);
            font-weight: 600;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        ul {
            list-style: none;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
        }

        /* Hero Banner */
        .hero-banner {
            background-color: #000000;
            padding: 100px 0;
            text-align: center;
            background-image: linear-gradient(rgba(255, 255, 255, 0.6), rgba(255, 255, 255, 0.4)),
                url('https://tse2.mm.bing.net/th/id/OIP.OiVz8r9d0sJGALjj8Xbn7AHaEK?pid=Api&P=0&h=220');
            background-size: cover;
            background-position: center;
            border-radius: 8px;
            margin: 20px auto;
            max-width: 1200px;
            box-shadow: 0 4px 8px rgba(168, 211, 178, 0.1);
        }

        .hero-content h1 {
            font-size: 48px;
            margin-bottom: 15px;
            letter-spacing: 1px;
        }

        .hero-content p {
            font-size: 18px;
            max-width: 700px;
            margin: 0 auto 30px;
            color: #000000;
            text-shadow: 1px 1px 2px rgba(102, 102, 102, 0.5);
        }

        .shop-now-btn {
            display: inline-block;
            padding: 12px 30px;
            background-color: var(--primary);
            color: var(--white);
            border-radius: 4px;
            font-weight: 500;
            transition: background-color 0.3s ease;
        }

        .shop-now-btn:hover {
            background-color: var(--secondary);
        }

        /* Featured Products */
        .featured-products {
            padding: 80px 0;
            background-color: var(--white);
        }

        .section-title {
            font-size: 32px;
            text-align: center;
            margin-bottom: 40px;
            position: relative;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 60px;
            height: 2px;
            background-color: var(--primary);
            margin: 15px auto 0;
        }

        .product-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 40px;
        }

        .product-card {
            background-color: var(--white);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            flex: 1 1 calc(50% - 10px);
            max-width: calc(50% - 10px);
            min-width: 0;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .product-card .category-label {
            font-size: 12px;
            color: var(--text);
            margin-bottom: 10px;
            text-transform: capitalize;
        }

        .product-card img {
            width: 100%;
            height: 180px;
            object-fit: cover;
            margin-bottom: 15px;
            border-radius: 4px;
        }

        .product-info h3 {
            font-size: 16px;
            margin-bottom: 10px;
            font-family: 'Playfair Display', serif;
        }

        .price {
            font-weight: 600;
            color: var(--primary);
            margin-bottom: 10px;
            font-size: 16px;
        }

        .old-price {
            text-decoration: line-through;
            color: var(--text);
            margin-left: 5px;
            font-size: 12px;
        }

        .rating {
            color: #FFD700;
            margin-bottom: 15px;
            font-size: 12px;
        }

        .add-to-cart {
            width: 100%;
            padding: 10px 0;
            background-color: var(--primary);
            color: var(--white);
            border: none;
            font-family: 'Jost', sans-serif;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s ease;
            border-radius: 4px;
            font-size: 14px;
        }

        .add-to-cart:hover {
            background-color: var(--secondary);
        }

        /* Responsive Styles */
        @media (min-width: 768px) {
            .product-card {
                flex: 1 1 calc(33.33% - 13.33px);
                max-width: calc(33.33% - 13.33px);
            }
        }

        @media (max-width: 768px) {
            .hero-content h1 {
                font-size: 36px;
            }

            .section-title {
                font-size: 28px;
            }
        }

        @media (max-width: 576px) {
            .hero-banner {
                padding: 60px 0;
            }

            .hero-content h1 {
                font-size: 28px;
            }

            .product-card {
                flex: 1 1 100%;
                max-width: 100%;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <section class="hero-banner">
            <div class="container">
                <div class="hero-content">
                    <h1>Fresh Stocks and Healthy Discounts</h1>
                    <p>Free delivery on all orders over $50</p>
                    <a href="/todays-deals" class="shop-now-btn">Shop Now</a>
                </div>
            </div>
        </section>

        <section class="featured-products">
            <div class="container">
                <h2 class="section-title">Featured Products</h2>
                <% if (error) { %>
                    <div class="error-message" style="color: #e74c3c; text-align: center; margin-bottom: 20px;">
                        <%= error %>
                    </div>
                    <% } %>
                        <div class="product-grid">
                            <% products.forEach(product=> { %>
                                <div class="product-card" data-category="<%= product.category %>"
                                    data-name="<%= product.name.toLowerCase() %>">
                                    <div class="category-label">
                                        <%= product.category %>
                                    </div>
                                    <img src="<%= product.image %>" alt="<%= product.name %>">
                                    <div class="product-info">
                                        <h3>
                                            <%= product.name %>
                                        </h3>
                                        <div class="price">
                                            $<%= product.price.toFixed(2) %>
                                                <% if (product.oldPrice) { %>
                                                    <span class="old-price">$<%= product.oldPrice.toFixed(2) %></span>
                                                    <% } %>
                                        </div>
                                        <div class="rating">
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star-half-alt"></i>
                                        </div>
                                        <button class="add-to-cart">Add to Cart</button>
                                    </div>
                                </div>
                                <% }) %>
                        </div>
            </div>
        </section>

        <%- include('partials/footer') %>
            <script src="/assets/js/home.js?v=<%= new Date().getTime() %>"></script>
</body>

</html>